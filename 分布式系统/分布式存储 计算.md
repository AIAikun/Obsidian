# **HDFS**
- 场景：存 100TB 的日志数据，单台机器存不下，怎么分布式存储？
- 逻辑：分 “NameNode（管理文件目录）+ DataNode（存实际数据，数据分片 + 多副本）”，比如一个 10GB 的文件分成 10 个 1GB 的块，每个块存 3 个副本，保证数据不丢。

# MapReduce

- 场景：统计 100TB 日志中每个用户的访问次数，单台机器算太慢，怎么分布式计算？
- 逻辑：分 “Map（把数据分片，每个节点统计局部的用户访问数）→ Reduce（把所有节点的局部结果汇总，得到全局用户访问数）”